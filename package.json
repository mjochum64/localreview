{
  "name": "localreview",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Code-Reviews gegen ein lokal laufendes Modell in Claude Code.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "smoke": "node --test tests/smoke/*.test.mjs"
  }
}
